# Collect

A `Dispatcher` `Module` to send events to the Tealium platform.

Configure settings programmatically by calling `Modules.collect(forcingSettings:)` and setting your preferred `CollectSettingsBuilder`.