# TealiumData

A `Collector` `Module` that enriches the collected data with Tealium specific information like the account and profile used to initialize the `Tealium` instance.

This module cannot be disabled.